const http = require('http'); const server = http.createServer((req, res) => { res.writeHead(200); res.end('Hello from direct deploy!'); }); server.listen(process.env.PORT || 3978, () => console.log('Server running'));
